本目录提供纯工具函数（布局计算、文案/标签等），不做 IO，不直接依赖 React。
输入：基础数据结构；输出：可测试的纯计算结果/显示辅助值。
更新规则：工具函数签名或职责变化时，更新本 README + `client/src/README.md`。

### 文件

- `gridLayout.js`：虚拟网格布局计算（列数、尺寸、位置等）。
- `mediaLabel.js`：媒体类型/展示标签计算与格式化。


