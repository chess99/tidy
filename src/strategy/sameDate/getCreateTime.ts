import { FileObj } from "../../typings"

function createTimeFromFileName(fileObj: FileObj): number {
  return 0
}

function createTimeFromExif(fileObj: FileObj): number {
  return 0
}


function createTimeFromFsStat(fileObj: FileObj): number {
  return 0
}