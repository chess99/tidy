---
description: "内部项目：优先最优实现，允许破坏性改动，拒绝兼容包袱与技术负债"
globs:
  - "**/*"
alwaysApply: true
---

## 核心原则（必须遵守）

- **只服务内部使用**：不需要考虑外部用户/第三方集成的兼容性与迁移成本。
- **永远选最优实现**：以长期可维护性、清晰性、性能与一致性为首要目标。
- **不留技术负债**：不要为了“先跑起来/以后再改”而引入临时方案、兼容层、TODO、弃用路径、双写逻辑等。

## 允许且鼓励的改动方式

- **可以破坏性重构**：接口/数据结构/路由/组件/模块边界不合理时，直接重做为更合理的形态。
- **删旧立新**：删除冗余代码、死代码、过时配置、无用开关；不要保留“以防万一”的分支。
- **统一规范**：命名、目录结构、错误处理、日志、返回值形状、状态管理方式保持一致。

## 变更必须同时完成的收尾（不做=等于留债）

- **全链路更新调用方**：改了接口/函数签名/返回结构，必须同步更新所有调用点与相关文档。
- **数据迁移/修复**：涉及持久化数据（DB/文件/缓存）结构变更时，提供一次性迁移脚本或自动修复逻辑，并在仓库内可复现执行。
- **移除旧路径**：不要保留 deprecated/legacy 入口、兼容适配器、影子字段、双格式支持。
- **清理与验证**：确保构建/启动流程可用；新增或更新最关键的测试/脚本来覆盖变更后的行为（仅保留高信噪比用例）。

## 明确禁止（常见“负债制造机”）

- **兼容层**：为了不改调用方而加适配器/别名/双协议/双 schema。
- **临时开关**：feature flag、回滚开关、A/B 分支仅用于掩盖设计问题。
- **“之后再说”**：在代码里留 TODO/FIXME/临时 hack，或用注释解释不合理实现而不改。

## 决策提示（遇到取舍时）

- **优先删减复杂度**：能通过重构与删除降低复杂度，就不要通过叠加逻辑“补丁式修复”。
- **优先一致性与可读性**：内部项目最贵的是维护成本；保持一眼能懂、边界清晰。

